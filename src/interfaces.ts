export interface CollectionCardDataInterface {
    id: number;
    title: string;
    image: string;
    raiting: number;
    minPlayers: number;
    maxPlayers: number;
    duration: number;
    difficulty: number;
}
